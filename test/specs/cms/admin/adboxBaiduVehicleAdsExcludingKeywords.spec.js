let AdboxPage = require('../../../pageobjects/cms/admin/adbox.page');

describe('Admin Adbox Baidu > Vehicle Ads > Excluding Keywords', function () {
    let adboxPage;

    before(function () {
        adboxPage = new AdboxPage();
    });

    it ('should see how', function () {
        expect('abc').to.equal('abc');
    });
});